<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="egoo.insertlog">

    <insert id="greeting" parameterType="java.util.Map">
        insert into greeting(time,method) values(#{time},#{method});
    </insert>

</mapper>